endpoints:
  unauthenticated:
    general:
      - /general/**
    internal:
      - /internal/**
    product:
      - /product/**
    auth:
      - /auth/**
    search:
      - /search/**
  verified:
    paths:
      - /aws/**
