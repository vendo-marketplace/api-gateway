type: object
properties:
  message:
    type: string
    example: Validation failed.
  errors:
    type: object
    additionalProperties:
      type: string
    example:
      email: Invalid email.
      password: Invalid password.
  code:
    type: integer
    example: 400
  path:
    type: string
    example: /auth/sign-up

