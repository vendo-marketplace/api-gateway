spring:
  application:
    name: api-gateway
  profiles:
    active: ${APP_PROFILE}
  config:
    import: "optional:configserver:${CONFIG_SERVER_URL}"

springdoc:
  swagger-ui:
    url: /openapi.yaml
  api-docs:
    enabled: false